{
  "name": "flatpack-repo",
  "version": "0.4.0-alpha",
  "private": true,
  "description": "Repo-level tooling for the Flatpack standard. Flatpack artifacts themselves are zero-dependency single HTML files — this package.json exists only for reviewer-side scripts under tools/.",
  "license": "MIT",
  "scripts": {
    "check": "node tools/check-flatpack.mjs --strict",
    "smoke": "node tools/browser-smoke-test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
